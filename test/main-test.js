const test = require('tape');

const satire = require('../');

test('Should start an HTTP server', (assert) => {
    // on the port specified by SATIRE_PORT
    // on a random availble port
    // and emit the port it is running on in an event
});
